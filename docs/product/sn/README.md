# SN WBS Discovery

This folder contains SN white-label delivery discovery artifacts.

- `SN_WBS_Discovery.md` maps the SN WBS to reusable isA platform assets.
- `wbs.xlsx` is the source WBS workbook with effort, role, module, and cost worksheets.

Keep these with product/delivery documentation rather than in the workspace root because they span multiple implementation repos.
